import { IColor } from "./types/IColor.type";

export const defaultCardColor = (): IColor => ({ borderColor: "#CDCDCD" });
export const matchedCardsColor = (): IColor => ({ borderColor: "#0A0A0C" });
export const compareCardsColor = (): IColor => ({ borderColor: "#ED5517" });
